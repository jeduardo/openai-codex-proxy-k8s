---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: codex-auth
  namespace: ai
  labels:
    app.kubernetes.io/name: openai-codex-proxy
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 32Mi
